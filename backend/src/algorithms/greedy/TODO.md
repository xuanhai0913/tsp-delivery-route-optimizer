# Greedy Algorithm TODO

Owner: Member 1

- Implement nearest-neighbor TSP solver.
- Input: `start` index and square `costMatrix`.
- Output: route including return to start, total cost, and runtime.
- Document expected complexity: usually `O(n^2)`.
- Add focused tests for valid routes and cost calculation.
