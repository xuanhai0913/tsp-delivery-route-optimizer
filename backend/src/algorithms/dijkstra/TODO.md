# Dijkstra TODO

- Implement shortest-path solver for non-negative weighted graphs.
- Track distance map, predecessor map, visited order, and relaxed edges.
- Return `path`, `totalCost`, `runtimeMs`, `visitedOrder`, and `relaxedEdges`.
- Add tests comparing with known shortest paths.
