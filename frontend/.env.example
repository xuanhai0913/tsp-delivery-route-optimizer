# Local dev can leave this empty so Vite proxies /api to Render without CORS issues.
# Vercel environments should set this to https://routelab-backend.onrender.com.
VITE_API_BASE_URL=
VITE_ENABLE_MOCK_FALLBACK=true
VITE_API_TIMEOUT_MS=30000
